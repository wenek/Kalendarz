<resources>

    <!-- Domyślny motyw (niebieski) -->
    <style name="Theme.KalendarzSemi.Blue" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/blue_primary</item>
        <item name="colorPrimaryDark">@color/blue_primary_dark</item>
        <item name="colorAccent">@color/blue_accent</item>
    </style>

    <!-- Zielony motyw -->
    <style name="Theme.KalendarzSemi.Green" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/green_primary</item>
        <item name="colorPrimaryDark">@color/green_primary_dark</item>
        <item name="colorAccent">@color/green_accent</item>
    </style>

    <!-- Czerwony motyw -->
    <style name="Theme.KalendarzSemi.Red" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/red_primary</item>
        <item name="colorPrimaryDark">@color/red_primary_dark</item>
        <item name="colorAccent">@color/red_accent</item>
    </style>

</resources>
